diff --git a/chrome/app/chrome_command_ids.h b/chrome/app/chrome_command_ids.h
index b36306e291..4c32a7eab3 100644
--- a/chrome/app/chrome_command_ids.h
+++ b/chrome/app/chrome_command_ids.h
@@ -362,6 +362,7 @@
 #define IDC_CONTENT_CONTEXT_COPYLINKTEXT 50107
 #define IDC_CONTENT_CONTEXT_OPENLINKINPROFILE 50108
 #define IDC_CONTENT_CONTEXT_OPENLINKBOOKMARKAPP 50109
+#define IDC_CONTENT_CONTEXT_OPENLINKPREVIEW 50110
 #define IDC_CONTENT_CONTEXT_OPENLINKSPLITVIEW 50111
 // Image items.
 #define IDC_CONTENT_CONTEXT_SAVEIMAGEAS 50120
diff --git a/chrome/app/generated_resources.grd b/chrome/app/generated_resources.grd
index b3263e1207..0fbc9fff94 100644
--- a/chrome/app/generated_resources.grd
+++ b/chrome/app/generated_resources.grd
@@ -666,6 +666,9 @@ are declared in tools/grit/grit_args.gni.
           <message name="IDS_CONTENT_CONTEXT_OPENLINKNEWTAB" desc="The name of the Open Link in New Tab command in the content area context menu">
             Open link in new &amp;tab
           </message>
+          <message name="IDS_CONTENT_CONTEXT_OPENLINKPREVIEW" desc="The name of the command that opens a link in an ephemeral preview surface">
+            Preview link
+          </message>
           <message name="IDS_CONTENT_CONTEXT_OPENLINKNEWWINDOW" desc="The name of the Open Link in New Window command in the content area context menu">
             Open link in new &amp;window
           </message>
@@ -973,6 +976,9 @@ are declared in tools/grit/grit_args.gni.
           <message name="IDS_CONTENT_CONTEXT_OPENLINKNEWTAB" desc="In Title Case: The name of the Open Link in New Tab command in the content area context menu">
             Open Link in New &amp;Tab
           </message>
+          <message name="IDS_CONTENT_CONTEXT_OPENLINKPREVIEW" desc="In Title Case: The name of the command that opens a link in an ephemeral preview surface">
+            Preview Link
+          </message>
           <message name="IDS_CONTENT_CONTEXT_OPENLINKNEWWINDOW" desc="In Title Case: The name of the Open Link in New Window command in the content area context menu">
             Open Link in New &amp;Window
           </message>
diff --git a/chrome/browser/BUILD.gn b/chrome/browser/BUILD.gn
index 63c2601cfc..9d6de31546 100644
--- a/chrome/browser/BUILD.gn
+++ b/chrome/browser/BUILD.gn
@@ -295,6 +295,13 @@ static_library("browser") {
   allow_circular_includes_from = [
     # TODO(crbug.com/503408628): Remove this circular dependency.
     "//chrome/browser/first_run:impl",
+    "//seoul/browser/lifecycle",
+    "//seoul/browser/organization",
+    "//seoul/browser/commands:command_chromium",
+    "//seoul/browser/projection:projection_chromium",
+    "//seoul/browser/shell:shell_chromium",
+    "//seoul/browser/product/browser:product_chromium",
+    "//seoul/browser/canvas",
 
     "//chrome/browser/browsing_data:impl",
     "//chrome/browser/accessibility:impl",
@@ -912,6 +919,13 @@ static_library("browser") {
     "//chrome/browser/profiles:profile_io_data",
     "//chrome/browser/profiles:profile_manager",
     "//chrome/browser/profiles:profile_manager_impl",
+    "//seoul/browser/lifecycle",
+    "//seoul/browser/organization",
+    "//seoul/browser/commands:command_chromium",
+    "//seoul/browser/projection:projection_chromium",
+    "//seoul/browser/shell:shell_chromium",
+    "//seoul/browser/product/browser:product_chromium",
+    "//seoul/browser/canvas",
     "//chrome/browser/profiles:profile_util",
     "//chrome/browser/profiles:profile_util_impl",
     "//chrome/browser/profiles:profiles_extra_parts",
diff --git a/chrome/browser/profiles/chrome_browser_main_extra_parts_profiles.cc b/chrome/browser/profiles/chrome_browser_main_extra_parts_profiles.cc
index 1aab47b6d0..9e6a4664d1 100644
--- a/chrome/browser/profiles/chrome_browser_main_extra_parts_profiles.cc
+++ b/chrome/browser/profiles/chrome_browser_main_extra_parts_profiles.cc
@@ -13,6 +13,7 @@
 #include "build/chromeos_buildflags.h"
 #include "chrome/browser/accessibility/accessibility_labels_service_factory.h"
 #include "chrome/browser/accessibility/page_colors_controller_factory.h"
+#include "seoul/browser/organization/seoul_organization_service_factory.h"
 #include "chrome/browser/accessibility_annotator/accessibility_annotator_backend_factory.h"
 #include "chrome/browser/accessibility_annotator/accessibility_annotator_enablement_service_factory.h"
 #include "chrome/browser/accessibility_annotator/accessibility_annotator_service_factory.h"
@@ -31,6 +32,7 @@
 #include "chrome/browser/autocomplete/provider_state_service_factory.h"
 #include "chrome/browser/autocomplete/shortcuts_backend_factory.h"
 #include "chrome/browser/autofill/autocomplete_history_manager_factory.h"
+#include "seoul/browser/product/browser/seoul_runtime_service_factory.h"
 
 #if BUILDFLAG(IS_WIN) || BUILDFLAG(IS_MAC) || BUILDFLAG(IS_LINUX) || \
     BUILDFLAG(IS_CHROMEOS)
@@ -1514,6 +1516,8 @@ void ChromeBrowserMainExtraPartsProfiles::
 
   WebDataServiceFactory::GetInstance();
   webrtc_event_logging::WebRtcEventLogManagerKeyedServiceFactory::GetInstance();
+  seoul::SeoulOrganizationServiceFactory::GetInstance();
+  seoul::SeoulRuntimeServiceFactory::GetInstance();
 }
 
 void ChromeBrowserMainExtraPartsProfiles::PreProfileInit() {
diff --git a/chrome/browser/renderer_context_menu/render_view_context_menu.cc b/chrome/browser/renderer_context_menu/render_view_context_menu.cc
index 556579d8e4..27e224a671 100644
--- a/chrome/browser/renderer_context_menu/render_view_context_menu.cc
+++ b/chrome/browser/renderer_context_menu/render_view_context_menu.cc
@@ -232,6 +232,9 @@
 #include "services/metrics/public/cpp/ukm_builders.h"
 #include "services/metrics/public/cpp/ukm_recorder.h"
 #include "services/service_manager/public/cpp/interface_provider.h"
+#include "seoul/browser/product/browser/seoul_runtime_service.h"
+#include "seoul/browser/product/browser/seoul_runtime_service_factory.h"
+#include "seoul/browser/preview/preview_host_service.h"
 #include "third_party/blink/public/common/associated_interfaces/associated_interface_provider.h"
 #include "third_party/blink/public/common/context_menu_data/context_menu_data.h"
 #include "third_party/blink/public/common/context_menu_data/edit_flags.h"
@@ -602,13 +605,14 @@ const std::map<int, int>& GetIdcToUmaMap(UmaEnumIdLookupType type) {
        {IDC_CONTENT_CONTEXT_GLIC, 162},
        {IDC_CONTENT_CONTEXT_VIDEO_FRAME, 163},
        {IDC_CONTENT_CONTEXT_LISTEN_TO_THIS_PAGE, 164},
+       {IDC_CONTENT_CONTEXT_OPENLINKPREVIEW, 165},
        // To add new items:
        //   - Add one more line above this comment block, using the UMA value
        //     from the line below this comment block.
        //   - Increment the UMA value in that latter line.
        //   - Add the new item to the RenderViewContextMenuItem enum in
        //     tools/metrics/histograms/metadata/ui/enums.xml.
-       {0, 165}});
+       {0, 166}});
   // LINT.ThenChange(//tools/metrics/histograms/metadata/ui/enums.xml:RenderViewContextMenuItem)
 
   // LINT.IfChange(ContextMenuOptionDesktop)
@@ -647,13 +651,14 @@ const std::map<int, int>& GetIdcToUmaMap(UmaEnumIdLookupType type) {
        {IDC_CONTENT_CONTEXT_SEARCHWEBFORNEWTAB, 29},
        {IDC_CONTENT_CONTEXT_OPENLINKSPLITVIEW, 31},
        {IDC_CONTENT_CONTEXT_GLICSHAREIMAGE, 32},
+       {IDC_CONTENT_CONTEXT_OPENLINKPREVIEW, 33},
        // To add new items:
        //   - Add one more line above this comment block, using the UMA value
        //     from the line below this comment block.
        //   - Increment the UMA value in that latter line.
        //   - Add the new item to the ContextMenuOptionDesktop enum in
        //     tools/metrics/histograms/metadata/enums.xml.
-       {0, 32}});
+       {0, 34}});
   // LINT.ThenChange(//tools/metrics/histograms/metadata/ui/enums.xml:ContextMenuOptionDesktop)
 
   return *(type == UmaEnumIdLookupType::GeneralEnumId ? kGeneralMap
@@ -716,6 +721,7 @@ int FindUMAEnumValueForCommand(int id, UmaEnumIdLookupType type) {
 // Returns true if the command id is for opening a link.
 bool IsCommandForOpenLink(int id) {
   return id == IDC_CONTENT_CONTEXT_OPENLINKNEWTAB ||
+         id == IDC_CONTENT_CONTEXT_OPENLINKPREVIEW ||
          id == IDC_CONTENT_CONTEXT_OPENLINKNEWWINDOW ||
          id == IDC_CONTENT_CONTEXT_OPENLINKOFFTHERECORD ||
          id == IDC_CONTENT_CONTEXT_OPENLINKSPLITVIEW ||
@@ -1798,6 +1804,14 @@ void RenderViewContextMenu::AppendLinkItems() {
                  : IDS_CONTENT_CONTEXT_OPENLINKNEWTAB);
     }
 
+    Browser* const browser = GetBrowser();
+    if (show_open_in_new_tab && browser && browser->is_type_normal() &&
+        !GetProfile()->IsOffTheRecord() &&
+        params_.link_url.SchemeIsHTTPOrHTTPS()) {
+      menu_model_.AddItemWithStringId(IDC_CONTENT_CONTEXT_OPENLINKPREVIEW,
+                                      IDS_CONTENT_CONTEXT_OPENLINKPREVIEW);
+    }
+
     if (show_open_in_new_window) {
       menu_model_.AddItemWithStringId(IDC_CONTENT_CONTEXT_OPENLINKNEWWINDOW,
                                       IDS_CONTENT_CONTEXT_OPENLINKNEWWINDOW);
@@ -3005,6 +3019,13 @@ bool RenderViewContextMenu::IsCommandIdEnabled(int id) const {
       return navigation_allowed && params_.link_url.is_valid() &&
              IsOpenLinkAllowedByDlp(params_.link_url);
 
+    case IDC_CONTENT_CONTEXT_OPENLINKPREVIEW:
+      return navigation_allowed && GetBrowser() &&
+             GetBrowser()->is_type_normal() &&
+             !GetProfile()->IsOffTheRecord() &&
+             params_.link_url.SchemeIsHTTPOrHTTPS() &&
+             IsOpenLinkAllowedByDlp(params_.link_url);
+
     case IDC_CONTENT_CONTEXT_OPENLINKSPLITVIEW:
       return navigation_allowed && params_.link_url.is_valid() &&
              params_.link_url.IsStandard() &&
@@ -3321,6 +3342,15 @@ void RenderViewContextMenu::ExecuteCommand(int id, int event_flags) {
           /*started_from_context_menu=*/true);
       break;
 
+    case IDC_CONTENT_CONTEXT_OPENLINKPREVIEW:
+      if (seoul::SeoulRuntimeService* runtime =
+              seoul::SeoulRuntimeServiceFactory::GetForProfile(GetProfile())) {
+        if (seoul::PreviewHostService* preview = runtime->preview_host()) {
+          preview->OpenFromLink(source_web_contents_, params_.link_url);
+        }
+      }
+      break;
+
     case IDC_CONTENT_CONTEXT_OPENLINKOFFTHERECORD:
       // Pass along the |referring_url| so we can show it in browser UI. Note
       // that this won't and shouldn't be sent via the referrer header.
diff --git a/chrome/browser/resources/BUILD.gn b/chrome/browser/resources/BUILD.gn
index b66098b14f..0584d50bea 100644
--- a/chrome/browser/resources/BUILD.gn
+++ b/chrome/browser/resources/BUILD.gn
@@ -81,6 +81,7 @@ group("resources") {
       "side_panel/history_clusters:resources",
       "side_panel/read_anything:resources",
       "side_panel/reading_list:resources",
+      "//seoul/browser/canvas/resources:resources",
       "side_panel/shared:resources",
       "side_panel/tabs_from_other_devices:resources",
       "signin:resources",
diff --git a/chrome/browser/ui/side_panel/side_panel_entry_id.h b/chrome/browser/ui/side_panel/side_panel_entry_id.h
index ac0e2c2d6a..7a32d68825 100644
--- a/chrome/browser/ui/side_panel/side_panel_entry_id.h
+++ b/chrome/browser/ui/side_panel/side_panel_entry_id.h
@@ -42,6 +42,7 @@
   V(kGlic, kActionSidePanelShowGlic, "Glic")                                  \
   V(kTabsFromOtherDevices, kActionSidePanelShowTabsFromOtherDevices,          \
     "TabsFromOtherDevices")                                                   \
+  V(kSeoulCanvas, std::nullopt, "SeoulCanvas")                                \
   V(kSidePanelDev, std::nullopt, "SidePanelDev")                              \
   /* Extensions (nothing more should be added below here) */                  \
   V(kExtension, std::nullopt, "Extension")
diff --git a/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.cc b/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.cc
index 25ffbdf18e..046ffd0eac 100644
--- a/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.cc
+++ b/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.cc
@@ -49,6 +49,12 @@
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_controller.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_top_container.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_view.h"
+#include "components/sessions/core/session_id.h"
+#include "seoul/browser/lifecycle/lifecycle_identity.h"
+#include "seoul/browser/organization/seoul_organization_service.h"
+#include "seoul/browser/organization/seoul_organization_service_factory.h"
+#include "seoul/browser/projection/projection_service.h"
+#include "seoul/browser/shell/shell_service.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_unpinned_tab_container_view.h"
 #include "chrome/browser/ui/web_applications/app_browser_controller.h"
@@ -292,6 +298,27 @@ VerticalTabStripRegionView::~VerticalTabStripRegionView() {
   hover_locks_.clear();
 
   if (root_node_) {
+    if (browser_view_ && browser_view_->browser()) {
+      Profile* profile = browser_view_->browser()->profile();
+      if (seoul::SeoulOrganizationService* service =
+              seoul::SeoulOrganizationServiceFactory::GetForProfile(profile)) {
+        if (seoul::ProjectionService* projection =
+                service->projection_service()) {
+          const SessionID sid = browser_view_->browser()->session_id();
+          if (sid.is_valid()) {
+            projection->UnregisterVerticalRegion(
+                seoul::LiveWindowKey::FromSessionId(sid.id()));
+          }
+        }
+        if (seoul::ShellService* shell = service->shell_service()) {
+          const SessionID sid = browser_view_->browser()->session_id();
+          if (sid.is_valid()) {
+            shell->UnregisterVerticalRegion(
+                seoul::LiveWindowKey::FromSessionId(sid.id()));
+          }
+        }
+      }
+    }
     root_node_->SetController(nullptr);
   }
 
@@ -505,6 +532,21 @@ void VerticalTabStripRegionView::Layout(PassKey) {
 }
 
 views::View* VerticalTabStripRegionView::GetDefaultFocusableChild() {
+  if (browser_view_ && browser_view_->browser()) {
+    Profile* profile = browser_view_->browser()->profile();
+    if (seoul::SeoulOrganizationService* service =
+            seoul::SeoulOrganizationServiceFactory::GetForProfile(profile)) {
+      if (seoul::ProjectionService* projection = service->projection_service()) {
+        const SessionID sid = browser_view_->browser()->session_id();
+        if (sid.is_valid()) {
+          if (views::View* child = projection->GetDefaultFocusableChildForWindow(
+                  seoul::LiveWindowKey::FromSessionId(sid.id()))) {
+            return child;
+          }
+        }
+      }
+    }
+  }
   const int active_index = tab_strip_model_->active_index();
   if (active_index != TabStripModel::kNoTab) {
     return GetTabAnchorViewAt(active_index);
@@ -662,6 +704,30 @@ void VerticalTabStripRegionView::InitializeTabStrip() {
   on_child_moved_subscription_ =
       root_node_->RegisterOnChildMovedCallback(base::BindRepeating(
           &VerticalTabStripRegionView::OnChildMoved, base::Unretained(this)));
+
+  // Register Seoul projection and shell ONLY after the root node is fully
+  // initialized (controller set, Init() called, child callbacks installed), so
+  // they never observe a partially initialized view tree. Registration applies
+  // the current projection even when live state was published earlier.
+  if (browser_view_ && browser_view_->browser()) {
+    Profile* profile = browser_view_->browser()->profile();
+    if (seoul::SeoulOrganizationService* service =
+            seoul::SeoulOrganizationServiceFactory::GetForProfile(profile)) {
+      const SessionID sid = browser_view_->browser()->session_id();
+      if (sid.is_valid()) {
+        const seoul::LiveWindowKey key =
+            seoul::LiveWindowKey::FromSessionId(sid.id());
+        if (seoul::ProjectionService* projection =
+                service->projection_service()) {
+          projection->RegisterVerticalRegion(key, this);
+        }
+        if (seoul::ShellService* shell = service->shell_service()) {
+          shell->RegisterVerticalRegion(key, this,
+                                        browser_view_->browser());
+        }
+      }
+    }
+  }
 }
 
 void VerticalTabStripRegionView::ResetTabStrip() {
@@ -669,6 +735,27 @@ void VerticalTabStripRegionView::ResetTabStrip() {
     return;
   }
 
+  // Unregister Seoul BEFORE the subscriptions, controller, root node and child
+  // views are reset, so no adapter or host retains a resetting root node.
+  if (browser_view_ && browser_view_->browser()) {
+    Profile* profile = browser_view_->browser()->profile();
+    if (seoul::SeoulOrganizationService* service =
+            seoul::SeoulOrganizationServiceFactory::GetForProfile(profile)) {
+      const SessionID sid = browser_view_->browser()->session_id();
+      if (sid.is_valid()) {
+        const seoul::LiveWindowKey key =
+            seoul::LiveWindowKey::FromSessionId(sid.id());
+        if (seoul::ProjectionService* projection =
+                service->projection_service()) {
+          projection->UnregisterVerticalRegion(key);
+        }
+        if (seoul::ShellService* shell = service->shell_service()) {
+          shell->UnregisterVerticalRegion(key);
+        }
+      }
+    }
+  }
+
   on_children_added_subscription_.reset();
   on_children_removed_subscription_.reset();
   on_child_moved_subscription_.reset();
@@ -1110,6 +1197,20 @@ void VerticalTabStripRegionView::OnCollapseStateChanged(
     tab_strip_view_->SetCollapsedState(collapsed);
   }
 
+  if (browser_view_ && browser_view_->browser()) {
+    Profile* profile = browser_view_->browser()->profile();
+    if (seoul::SeoulOrganizationService* service =
+            seoul::SeoulOrganizationServiceFactory::GetForProfile(profile)) {
+      if (seoul::ShellService* shell = service->shell_service()) {
+        const SessionID sid = browser_view_->browser()->session_id();
+        if (sid.is_valid()) {
+          shell->OnCollapseStateChanged(
+              seoul::LiveWindowKey::FromSessionId(sid.id()), collapsed);
+        }
+      }
+    }
+  }
+
   if (state == tabs::VerticalTabStripCollapseState::kExpanded ||
       state == tabs::VerticalTabStripCollapseState::kCollapsed) {
     UpdateExpandOnHoverState();
@@ -1608,5 +1709,15 @@ gfx::Rect VerticalTabStripRegionView::GetLinkDropBoundsFromPosition(
   return gfx::Rect(position, gfx::Size(DropArrow::kSize, DropArrow::kSize));
 }
 
+// Seoul shell anchors. Defined out-of-line because the concrete child types are
+// only complete in this translation unit.
+views::View* VerticalTabStripRegionView::GetSeoulShellSeparatorAnchor() {
+  return top_button_separator_;
+}
+
+views::View* VerticalTabStripRegionView::GetSeoulShellFooterAnchor() {
+  return bottom_button_container_;
+}
+
 BEGIN_METADATA(VerticalTabStripRegionView)
 END_METADATA
diff --git a/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h b/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h
index 70c78e23c0..5bd1b5a10b 100644
--- a/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h
+++ b/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h
@@ -187,6 +187,13 @@ class VerticalTabStripRegionView final
   tabs::VerticalTabStripState target_collapse_state_for_testing() {
     return target_collapse_state_;
   }
+  views::View* GetSeoulShellSeparatorAnchor();
+  views::View* GetSeoulShellFooterAnchor();
+  views::View* GetSeoulTabStripView() { return tab_strip_view_; }
+  // Production accessor for the initialized vertical-tab root node, used by the
+  // Seoul projection adapter (not a testing-only accessor). Non-null only
+  // between InitializeTabStrip() and ResetTabStrip().
+  RootTabCollectionNode* GetSeoulRootNode() { return root_node_.get(); }
   void DisableTabStripEditingForTesting() override;
   gfx::Rect GetLinkDropBoundsForTesting(
       const BrowserRootView::DropIndex& drop_index,
diff --git a/chrome/browser/ui/views/side_panel/BUILD.gn b/chrome/browser/ui/views/side_panel/BUILD.gn
index a17f190873..5cb78625bd 100644
--- a/chrome/browser/ui/views/side_panel/BUILD.gn
+++ b/chrome/browser/ui/views/side_panel/BUILD.gn
@@ -98,6 +98,7 @@ source_set("side_panel") {
     "//url",
   ]
   deps = [
+    "//seoul/browser/canvas",
     "//base:i18n",
     "//chrome/app:generated_resources",
     "//chrome/app:generated_resources_grit",
diff --git a/chrome/browser/ui/views/side_panel/side_panel_helper.cc b/chrome/browser/ui/views/side_panel/side_panel_helper.cc
index 9dfcb4b484..a366aae178 100644
--- a/chrome/browser/ui/views/side_panel/side_panel_helper.cc
+++ b/chrome/browser/ui/views/side_panel/side_panel_helper.cc
@@ -4,11 +4,14 @@
 
 #include "chrome/browser/ui/views/side_panel/side_panel_helper.h"
 
+#include "base/functional/bind.h"
+#include "seoul/browser/canvas/seoul_canvas_ui.h"
 #include "chrome/browser/history_clusters/history_clusters_service_factory.h"
 #include "chrome/browser/profiles/profile.h"
 #include "chrome/browser/ui/browser.h"
 #include "chrome/browser/ui/browser_actions.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_features.h"
+#include "chrome/browser/ui/side_panel/side_panel_entry.h"
 #include "chrome/browser/ui/side_panel/side_panel_registry.h"
 #include "chrome/browser/ui/side_panel/side_panel_ui.h"
 #include "chrome/browser/ui/ui_features.h"
@@ -17,11 +20,37 @@
 #include "chrome/browser/ui/views/side_panel/history/history_side_panel_coordinator.h"
 #include "chrome/browser/ui/views/side_panel/history_clusters/history_clusters_side_panel_coordinator.h"
 #include "chrome/browser/ui/views/side_panel/reading_list/reading_list_side_panel_coordinator.h"
+#include "chrome/browser/ui/views/side_panel/side_panel_web_ui_view.h"
 #include "chrome/browser/ui/views/side_panel/tabs_from_other_devices/tabs_from_other_devices_side_panel_coordinator.h"
+#include "chrome/browser/ui/webui/top_chrome/webui_contents_wrapper.h"
 #include "chrome/browser/ui/webui_browser/webui_browser.h"
+#include "chrome/grit/branded_strings.h"
+#include "chrome/grit/generated_resources.h"
 #include "components/history_clusters/core/features.h"
 #include "components/history_clusters/core/history_clusters_service.h"
 #include "ui/actions/actions.h"
+#include "ui/base/metadata/metadata_impl_macros.h"
+#include "url/gurl.h"
+
+using SidePanelWebUIViewT_SeoulCanvasUI =
+    SidePanelWebUIViewT<seoul::SeoulCanvasUI>;
+BEGIN_TEMPLATE_METADATA(SidePanelWebUIViewT_SeoulCanvasUI,
+                        SidePanelWebUIViewT)
+END_METADATA
+
+namespace {
+
+std::unique_ptr<views::View> CreateSeoulCanvasWebView(
+    Profile* profile,
+    SidePanelEntryScope& scope) {
+  return std::make_unique<SidePanelWebUIViewT<seoul::SeoulCanvasUI>>(
+      scope, base::RepeatingClosure(), base::RepeatingClosure(),
+      std::make_unique<WebUIContentsWrapperT<seoul::SeoulCanvasUI>>(
+          GURL("chrome://seoul-canvas"), profile, IDS_PRODUCT_NAME,
+          /*esc_closes_ui=*/false));
+}
+
+}  // namespace
 
 // static
 void SidePanelHelper::PopulateGlobalEntries(
@@ -41,6 +70,18 @@ void SidePanelHelper::PopulateGlobalEntries(
     return;
   }
 
+  // Seoul Canvas is profile-scoped but window-bound at creation time. It is
+  // registered once in each regular browser window; the Shell command opens
+  // this exact entry without constructing a tab-hosted Canvas.
+  if (browser->profile()->IsRegularProfile()) {
+    auto entry = std::make_unique<SidePanelEntry>(
+        SidePanelEntry::Key(SidePanelEntry::Id::kSeoulCanvas),
+        base::BindRepeating(&CreateSeoulCanvasWebView, browser->profile()),
+        /*default_content_width_callback=*/base::NullCallback());
+    entry->set_should_show_header(false);
+    window_registry->Register(std::move(entry));
+  }
+
   // Add tabs from other devices.
   if (TabsFromOtherDevicesSidePanelCoordinator::IsSupported(
           browser->profile())) {
diff --git a/chrome/browser/ui/webui/chrome_web_ui_configs.cc b/chrome/browser/ui/webui/chrome_web_ui_configs.cc
index 9daef52054..5e4ccc17f9 100644
--- a/chrome/browser/ui/webui/chrome_web_ui_configs.cc
+++ b/chrome/browser/ui/webui/chrome_web_ui_configs.cc
@@ -13,6 +13,7 @@
 #include "chrome/browser/ui/webui/about/about_ui.h"
 #include "chrome/browser/ui/webui/accessibility/accessibility_ui.h"
 #include "chrome/browser/ui/webui/accessibility_annotator_internals/accessibility_annotator_internals_ui.h"
+#include "seoul/browser/canvas/seoul_canvas_ui.h"
 #include "chrome/browser/ui/webui/actor_internals/actor_internals_ui.h"
 #include "chrome/browser/ui/webui/autofill_and_password_manager_internals/autofill_internals_ui.h"
 #include "chrome/browser/ui/webui/autofill_and_password_manager_internals/password_manager_internals_ui.h"
@@ -242,6 +243,7 @@ void RegisterChromeWebUIConfigs() {
 #endif  // BUILDFLAG(IS_CHROMEOS)
 
   auto& map = content::WebUIConfigMap::GetInstance();
+  map.AddWebUIConfig(std::make_unique<seoul::SeoulCanvasUIConfig>());
   map.AddWebUIConfig(std::make_unique<AccessibilityUIConfig>());
   map.AddWebUIConfig(
       std::make_unique<AccessibilityAnnotatorInternalsUIConfig>());
diff --git a/chrome/test/BUILD.gn b/chrome/test/BUILD.gn
index 5a75b29519..631c51b80d 100644
--- a/chrome/test/BUILD.gn
+++ b/chrome/test/BUILD.gn
@@ -2596,6 +2596,10 @@ if (!is_android) {
     defines = [ "HAS_OUT_OF_PROC_TEST_RUNNER" ]
 
     deps = [
+      "//seoul/browser/commands:command_browser_tests",
+      "//seoul/browser/product/browser:product_browser_tests",
+      "//seoul/browser/projection:projection_browser_tests",
+      "//seoul/browser/shell:shell_browser_tests",
       ":browser_tests_runner",
       ":platform_browser_tests",
       ":test_support",
diff --git a/third_party/lit/v3_0/BUILD.gn b/third_party/lit/v3_0/BUILD.gn
index 3d22db142f..33a4418d89 100644
--- a/third_party/lit/v3_0/BUILD.gn
+++ b/third_party/lit/v3_0/BUILD.gn
@@ -24,6 +24,7 @@ ts_library("build_ts") {
     # dependency to CrOS-only UIs (to reduce coupling between Desktop and CrOS
     # and reduce the maintenance burden for the Desktop WebUI team).
 
+    "//seoul/browser/canvas/resources:build_ts",
     "//chrome/browser/resources/about_sys:build_ts",
     "//chrome/browser/resources/access_code_cast:build_ts",
     "//chrome/browser/resources/accessibility_annotator_info:build_ts",
diff --git a/tools/gritsettings/resource_ids.spec b/tools/gritsettings/resource_ids.spec
index 9d69921f81..6ea5983549 100644
--- a/tools/gritsettings/resource_ids.spec
+++ b/tools/gritsettings/resource_ids.spec
@@ -1738,6 +1738,14 @@
     "messages": [10120],
   },
 
+  # Project Seoul first-party Canvas WebUI (//seoul/browser/canvas/resources).
+  # Sits in the "everything else" section (not chrome/components/content/ios);
+  # occupies the free range below the placeholder at 12000.
+  "<(SHARED_INTERMEDIATE_DIR)/seoul/browser/canvas/resources/resources.grd": {
+    "META": {"sizes": {"includes": [50],}},
+    "includes": [10140],
+  },
+
   "<(SHARED_INTERMEDIATE_DIR)/THIS_IS_A_PLACEHOLDER.grd": {
     "META": {"sizes": {"includes": [800]}},
     "includes": [12000],
diff --git a/tools/metrics/histograms/metadata/ui/enums.xml b/tools/metrics/histograms/metadata/ui/enums.xml
index f65a7cda7f..08a3d6825e 100644
--- a/tools/metrics/histograms/metadata/ui/enums.xml
+++ b/tools/metrics/histograms/metadata/ui/enums.xml
@@ -78,6 +78,7 @@ chromium-metrics-reviews@google.com.
   <int value="29" label="Search web for... in new tab"/>
   <int value="31" label="Open link in split view"/>
   <int value="32" label="Create image with Gemini in Chrome"/>
+  <int value="33" label="Preview link"/>
 </enum>
 
 <!-- LINT.ThenChange(//chrome/browser/renderer_context_menu/render_view_context_menu.cc:ContextMenuOptionDesktop) -->
@@ -363,6 +364,7 @@ chromium-metrics-reviews@google.com.
   <int value="162" label="IDC_CONTENT_CONTEXT_GLIC"/>
   <int value="163" label="IDC_CONTENT_CONTEXT_VIDEO_FRAME"/>
   <int value="164" label="IDC_CONTENT_CONTEXT_LISTEN_TO_THIS_PAGE"/>
+  <int value="165" label="IDC_CONTENT_CONTEXT_OPENLINKPREVIEW"/>
 </enum>
 
 <!-- LINT.ThenChange(//chrome/browser/renderer_context_menu/render_view_context_menu.cc:RenderViewContextMenuItem) -->
diff --git a/ui/views/bubble/bubble_dialog_delegate_view.h b/ui/views/bubble/bubble_dialog_delegate_view.h
index 4257c7ba1a..220bd0a471 100644
--- a/ui/views/bubble/bubble_dialog_delegate_view.h
+++ b/ui/views/bubble/bubble_dialog_delegate_view.h
@@ -101,6 +101,11 @@ class RichNudge;
 }  // namespace input_overlay
 }  // namespace arc
 
+namespace seoul {
+class CommandLauncherBubble;
+class SeoulPreviewBubbleView;
+}  // namespace seoul
+
 namespace ash {
 class AnchoredNudge;
 class ContextualNudge;
@@ -792,6 +797,8 @@ class VIEWS_EXPORT BubbleDialogDelegateView : public View,
   // These existing cases are "grandfathered in", but there shouldn't be more.
   // See comments atop class.
   friend class ::AccountChooserDialogView;
+  friend class seoul::CommandLauncherBubble;
+  friend class seoul::SeoulPreviewBubbleView;
   friend class ::AnnouncementView;
   friend class ::AppDialogView;
   friend class ::BruschettaUninstallerView;
diff --git a/ui/webui/resources/tools/build_webui.gni b/ui/webui/resources/tools/build_webui.gni
index 4b2eaadc7f..1703f64104 100644
--- a/ui/webui/resources/tools/build_webui.gni
+++ b/ui/webui/resources/tools/build_webui.gni
@@ -291,6 +291,11 @@ template("build_webui") {
     }
   }
 
+  # is_ash_webui is only consumed by the type-aware ESLint pass below; when a
+  # target sets enable_type_aware_eslint_checks=false that pass is skipped, so
+  # tell GN the variable may legitimately go unused.
+  not_needed([ "is_ash_webui" ])
+
   # Determine if there is a Lit and/or Polymer dependency. This information is
   # used for determining the default template for html_to_wrapper() as well as
   # the default tsconfig file for ts_library().
