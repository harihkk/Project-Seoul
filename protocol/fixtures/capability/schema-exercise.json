{
  "schema_version": 1,
  "id": "fixture.schema.exercise",
  "version": 2,
  "name": "schema exercise",
  "description": "exists to exercise every tool-schema field kind in conformance tests",
  "provider": "fixture",
  "input_schema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "maxLength": 256,
        "description": "free text"
      },
      "depth": {
        "type": "integer",
        "minimum": 1,
        "maximum": 5
      },
      "threshold": {
        "type": "number",
        "minimum": 0,
        "maximum": 1
      },
      "strict": {
        "type": "boolean"
      },
      "mode": {
        "type": "string",
        "enum": [
          "fast",
          "thorough"
        ]
      },
      "source": {
        "type": "string",
        "format": "uri"
      },
      "stations": {
        "type": "array",
        "items": {
          "type": "string",
          "maxLength": 16
        }
      },
      "window": {
        "type": "object",
        "properties": {
          "start_ms": {
            "type": "number"
          },
          "end_ms": {
            "type": "number"
          }
        },
        "required": [
          "start_ms"
        ]
      }
    },
    "required": [
      "query"
    ]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "result": {
        "type": "object"
      }
    },
    "required": [
      "result"
    ]
  },
  "capability_tags": [
    "schema",
    "exercise"
  ],
  "requires_network": false,
  "sensitivity": "none",
  "risk": "read_only",
  "approval": "never_required",
  "timeout_ms": 5000,
  "cancellable": true,
  "supports_streaming": false,
  "idempotency": "idempotent",
  "freshness": "static",
  "retry": {
    "max_attempts": 2,
    "backoff_ms": 250
  },
  "observation_contract": "None. This is a synthetic Design Lab fixture: execution returns a canned payload and no real browser or provider state is observed.",
  "verifier_id": ""
}
