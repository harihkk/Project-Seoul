# Seoul current state

This matrix records the local working tree as inspected on 2026-07-12. It is
evidence-first: no native Chromium target was compiled or launched on this host.
The sibling checkout at `/Users/hk/Documents/Projects/seoul-chromium.noindex`
(the `.noindex` suffix keeps Spotlight from indexing it) is a clean pinned
Chromium checkout used for include and patch verification. It has
no Project Seoul implementation source to merge; the only "seoul" filename
matches found there are upstream V8 timezone tests.

Host and checkout facts:

- Baseline commit under the working tree: `3adae470a6874b0515adf6a15bba33ef560624f6`.
- External Chromium checkout: `src` at `6a7b3dbec3b2ca25877c2553b5473b2f277ef644`, depot_tools at `1e85b18d8037bca45f9a9891ae9325729f3ebcd4`.
- `native/scripts/verify-checkout.sh` passes outside the sandbox; the sandboxed run blocks `gclient validate` on the vpython cache.
- `native/scripts/build-host-check.sh` fails on the real host: 8 GiB RAM and 62 GiB free storage are below the 16 GiB / 150 GiB minimums. Do not run GN or Ninja here.

Status vocabulary is restricted to: MISSING, INTERFACE ONLY, PARTIAL SOURCE,
SOURCE CONNECTED, SOURCE COMPLETE, COMPILED, TESTED, RUNTIME VERIFIED, RELEASE
VERIFIED.

| Subsystem | Intended responsibility | Production owner / constructor | Shutdown path | Scope | Persistent state | BUILD target | Chrome dependency path | Visible UI path | Tests | Compile status | Runtime status | Unresolved assumptions |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Organization model | Workspaces, scenes, essentials, routing, and durable browser organization state. | `SeoulOrganizationServiceFactory` constructs `SeoulOrganizationService`. | KeyedService shutdown tears down model, lifecycle, commands, projection, shell. | Profile, with live window keys per browser window. | Organization store prefs. | `//seoul/browser/organization`. | Registered in `chrome_browser_main_extra_parts_profiles.cc` patch. | Shell/projection consume it; no standalone UI. | Unit and browser-test source. | SOURCE CONNECTED | PARTIAL SOURCE | Native tests are authored but not compiled or run. |
| Lifecycle bridge | Publish live window/tab snapshots and remove stale windows. | `SeoulOrganizationService` owns `LiveWindowStateProvider` and watchers. | Observer removal and keyed-service teardown. | Window and tab session ids. | None; live only. | `//seoul/browser/lifecycle`. | Integrated through tab-strip/vertical-region patch paths. | Drives projection and runtime executors. | Unit-test source. | SOURCE COMPLETE | PARTIAL SOURCE | Needs capable-host browser tests for close/restore behavior. |
| Command executor | Submit typed browser commands and confirm outcomes through observation. | `SeoulOrganizationService` constructs `CommandExecutor` plus Chromium mutation adapter. | Completion observers removed, pending commands cancelled. | Profile plus target live window/tab. | Command ids are transient. | `//seoul/browser/commands:command_chromium`. | Browser-test deps added to `//chrome/test:browser_tests`. | Shell and task runtime call it. | Unit and browser-test source. | SOURCE CONNECTED | PARTIAL SOURCE | Needs compiled browser_tests for real tab-strip mutation evidence. |
| Projection | Convert organization/live state into vertical tab strip presentation. | `ProjectionService` from organization service. | Vertical region unregisters before reset/destruction. | Per window. | Projection is derived from model and live state. | `//seoul/browser/projection:projection_chromium`. | `VerticalTabStripRegionView` patch registers/unregisters the region. | Native vertical tab strip. | Unit and browser-test source. | SOURCE CONNECTED | PARTIAL SOURCE | Needs capable-host visual/runtime verification. |
| Seoul Shell | Workspace controls, cross-window Essentials, compact mode, explicit split chooser, searchable command launcher, and persistent Task Deck status inside Chromium Views. | `ShellService` and per-window `ShellController`; `SeoulRuntimeService` publishes minimized task counts. | Region host detaches during vertical region unregister/reset; runtime clears task summaries before task teardown. | Profile plus exact per-window controllers, live tab keys, and task counts. | Uses organization model; live presentation/task summary metadata is ephemeral. | `//seoul/browser/shell:shell_chromium`, bridged by product_chromium. | `VerticalTabStripRegionView` patch forwards anchors and collapse state. | Native vertical tab strip region; Shift+Cmd/Ctrl+K opens the launcher and the Task Deck control opens detailed Canvas tasks. | Pure launcher/view-model unit-test source, architecture gate, and browser-test source. | SOURCE CONNECTED | PARTIAL SOURCE | Searchable launcher, compact presentation, exact cross-window Essential activation, explicit split selection, window-owned accelerator, and minimized always-visible task status are source-wired; native multi-window and runtime accessibility/geometry/conflict evidence remain. |
| Product runtime | One profile-scoped owner for capabilities, providers, planner, tasks, surfaces, threads, workflows, page agent, and credentials. | `SeoulRuntimeServiceFactory` constructs `SeoulRuntimeService`. | Persists product pref, clears window bindings, shuts down tasks/providers/runtime in reverse dependency order. | Regular original profile; explicit window binding tokens for Canvas turns. | `seoul.product.v1`; secrets excluded. | `//seoul/browser/product/browser:product_chromium`. | Registered in `chrome_browser_main_extra_parts_profiles.cc` patch. | Indirect via Canvas, Task Deck, and shell. | Unit and browser-test source. | SOURCE CONNECTED | PARTIAL SOURCE | Needs native compilation and profile lifecycle/runtime tests. |
| Preview lifecycle | Keep link previews ephemeral and parent/window-bound, with explicit atomic promotion to tab or split. | `SeoulRuntimeService` owns `PreviewManager` and the Chromium-facing `PreviewHostService`. | Runtime dismisses the visible surface on exact parent-tab/window removal; replacement, crash, explicit dismissal, or committed promotion also removes the ephemeral record. | Exact live window + parent tab; at most one Preview per window and 32 profile-wide. | None by construction. | `//seoul/browser/preview:preview_core` and `:preview_chromium`. | The host resolves the exact normal window, owns a non-tab `WebContents`, embeds it in `SeoulPreviewWebView`, and transfers that same ownership through upstream tab/split APIs only after a retained-insertion handshake. Popups, downloads, dialogs, fullscreen, credential URLs, and non-HTTP(S) navigation are blocked. | Native bubble header with live loading/error state, Close, explicit Open as tab, and Move to split; promotion actions are disabled until ready. A localized, DLP-gated link context-menu command and the exact-scoped `browser.preview.open` AI capability both reach the same host. | Pure lifecycle unit-test source parses against real headers; non-tab open/dismiss browser-test source plus architecture, containment, accessibility, permission, patch round-trip, GRD, and UMA-enum gates are authored. | SOURCE CONNECTED | PARTIAL SOURCE | A faster direct modifier/hover gesture beyond the context menu, generated-header compilation, compiled promotion/split/context-menu tests, and runtime visual/focus/accessibility/reduced-motion evidence remain. |
| Window runtime binding | Bind a Canvas WebUI document to the exact browser window it may act on. | `SeoulRuntimeService::CreateWindowBinding` from `webui::GetBrowserWindowInterface`. | Token invalidated on Canvas teardown, explicit invalidation, service shutdown, or browser close callback. | Per profile and per browser window. | None; opaque runtime token only. | `//seoul/browser/product/browser:product_chromium`; Canvas depends on `//chrome/browser/ui/webui:webui_util`. | Chromium side-panel WebUI embedding context. | Canvas turns; tab-loaded Canvas fails closed as `window_unbound`. | Browser-test source plus product architecture gate. | SOURCE CONNECTED | PARTIAL SOURCE | Needs compiled multi-window side-panel/close tests. |
| Seoul Canvas WebUI | Render trusted SAUI surfaces and route typed turns/events into runtime. | `SeoulCanvasUI` creates `SeoulCanvasPageHandler`; `SeoulCanvasAppElement` owns the Lit view state. | Mojo receiver/remote reset, observers removed, realtime media stopped, window binding invalidated. | Profile plus bound browser window token. | Pinned surfaces live in runtime, not the handler. | `//seoul/browser/canvas` and `//seoul/browser/canvas/resources`. | Top-chrome WebUIConfig, generated Lit/resources, and one regular-profile `kSeoulCanvas` side-panel entry per window are registered by the patch; Shell receives the exact browser binding. | `chrome://seoul-canvas`, opened by the Shell command launcher; Canvas, Library, Boards, and read-only Studio views. | Host strict TypeScript check and architecture gate; browser-test source verifies WebUI config registration. | SOURCE CONNECTED | PARTIAL SOURCE | Responsive fixture QA passed before Studio; still needs capable-host generated-resource compile, entry/launcher browser tests, and real-Mojo Studio/visual/accessibility QA. |
| Library, Boards, Live Collections | Index durable browser/Seoul artifacts, spatial board documents, and provider-neutral refreshable collections. | `SeoulRuntimeService` owns `LibraryService`. | Runtime flushes coalesced persistence before Library teardown. | Profile; board and collection ids are durable UUIDs. | Bounded metadata in `seoul.product.v1`; binary bytes and credentials excluded. | `//seoul/browser/library:library_core`; Canvas Mojo/Lit resources expose bounded views. | Runtime service dependency only; capture and refresh adapters remain future slices. | Canvas Library view and Board index with create, archive/restore, and confirmation-gated delete. | Unit-test source, strict Canvas TypeScript check, domain-neutrality and product architecture gates. | SOURCE CONNECTED | PARTIAL SOURCE | Core parses clean locally; spatial editor, capture/refresh adapters, compiled Mojo/WebUI tests, and accessibility runtime evidence remain. |
| Studio index | Inspect profile reasoning and appearance registries without leaking provider internals. | `SeoulCanvasPageHandler` projects `ProviderRegistry`, `SceneRegistry`, `ThemeRegistry`, and `SiteLayerRegistry`; Lit owns view state only. | Canvas handler/window binding lifetime; no Studio-owned service or persistence. | Exact profile plus bound browser window. | Derived on request; Scene/Theme/Site Layer registries persist independently in the product pref. | `//seoul/browser/canvas`, with direct Scene/Theme/Site Layer deps. | Typed `GetStudioSnapshot` Mojo call into the profile runtime. | Canvas Studio view with provider-route, Scene, Theme-preview, and Site Layer cards/empty states. | Strict Canvas TypeScript, architecture, boundary, neutrality, and syntax gates. | SOURCE CONNECTED | PARTIAL SOURCE | Read-only and uncompiled; excludes secrets/endpoints/model ids/raw errors. Editors, mutation scheduling, activation/application, workflow/routing projections, visual QA, and runtime evidence remain. |
| SAUI and adaptive compiler | Validate/render semantic result shapes into domain-neutral visual surfaces. | `SurfaceService`, `saui` catalog, interface compiler. | Pure services reset in runtime shutdown. | Profile surfaces. | Pinned surface JSON in product pref. | `//seoul/browser/saui:saui_core`. | None beyond base/Seoul cores. | Lit Canvas renderer consumes SAUI JSON; every accepted chart/non-input map has local SVG plus accessible data. | Unit-test source plus catalog-derived renderer architecture gate. | SOURCE COMPLETE | PARTIAL SOURCE | Needs compiled unit tests and production-Mojo visual/accessibility QA. |
| Semantic data fabric | Domain-neutral schemas, roles, validation, inspection, and provenance. | `semantic` pure module. | Pure values only. | Capability result scope. | None except surfaces/threads storing results. | `//seoul/browser/semantic:semantic_core`. | None beyond base/Seoul cores. | Feeds SAUI and Canvas. | Unit-test source. | SOURCE COMPLETE | PARTIAL SOURCE | Needs compiled held-out-schema tests. |
| Capability and connector graph | Dynamic, versioned, provider-owned capabilities with availability and transport validation. | `SeoulRuntime` owns `ToolRegistry` and `ConnectorRegistry`. | Runtime shutdown disconnects registries. | Profile. | Provider settings in product pref. | `//seoul/browser/tools:tools_core`, `//seoul/browser/connectors:connectors_core`. | No direct Chrome dependency for core; browser catalog in product_chromium. | Planner/Task Deck/Canvas. | Unit-test source and CI architecture gates. | SOURCE COMPLETE | PARTIAL SOURCE | Real connector transports need runtime testing. |
| Capability executors and Task Deck | Plan, approve, collect bounded user input, execute, observe, verify, cancel, and recover tasks. | `TaskService` plus `CapabilityExecutorRegistry`. | Task shutdown cancels pending work; no late callbacks after shutdown. | Profile plus explicit window key per task. | Task summaries through product pref when persisted by runtime. | `//seoul/browser/product:product_core`; executors in product_chromium. | Browser/page executors depend on command layer and page agent. | Canvas task cards expose pause/resume, exact-step approval, and typed missing-input forms. | Unit-test source, task snapshot fixtures, strict Canvas TypeScript check, and product architecture gate. | SOURCE COMPLETE | PARTIAL SOURCE | Typed input is bounded, receipt-recorded, and replanned through the configured route; all task and Mojo paths still need compiled tests. |
| Agent permission policy | Fail closed before browser/page/connector execution and reuse consent only for an exact live scope. | `SeoulRuntimeService` owns `AgentPermissionService`, injects its committed-origin resolver into `TaskService`, and observes live window snapshots. | Grants revoke on tab removal, window removal, binding-close defense in depth, and runtime shutdown. | Capability + live window + live tab + main frame + source origin + optional destination origin + connector service. | None; grants are memory-only, bounded, and expire. | `//seoul/browser/policy:policy_core`; runtime and task targets depend on it. | Page scope resolves from the exact `WebContents` and `GetLastCommittedURL`; internal/non-HTTP(S) pages fail closed and a navigated origin cannot match the prior grant. | Canvas task card displays the minimized scope description at first use. | Pure adversarial unit-test source and product architecture gate. | SOURCE CONNECTED | PARTIAL SOURCE | Lifecycle revocation is source-wired; navigation/tab-close browser tests, injection/exfiltration tests, and compiled proof remain. |
| Provider registry and reasoning | Local-first reasoning, optional cloud provider, credentials, streaming protocol. | `ProviderRegistry` with injected local/cloud `HttpTransport` and `CredentialStore`. | Provider shutdown cancels health checks/streams. | Profile. | Provider configuration only; secrets in Keychain. | `//seoul/browser/product:product_core`, `//seoul/browser/intelligence:intelligence_core`. | Network and Keychain concrete classes in product_chromium. | Canvas status and planner route. | Unit-test source. | SOURCE CONNECTED | PARTIAL SOURCE | Real audio/model endpoint health needs runtime verification. |
| Page agent | Observe page semantics and perform typed accessibility actions without arbitrary script, while keeping form-control values and sensitive-field mutations outside the model path. | `PageAgent` owned by `SeoulRuntimeService`; pure `page_field_safety` classifies trusted AX/HTML signals. | Weak callbacks invalidated on shutdown. | Live tab key, resolved inside profile; handles expire by observation generation. | None. Current form values are not represented in `PageObservation` or its semantic projection. | `//seoul/browser/product:product_core` policy plus `//seoul/browser/product/browser:product_chromium` adapter. | WebContents resolver through `ProfileBrowserCollection`; bounded snapshots request Chromium's protected state and HTML `autocomplete` metadata. | Page capabilities via tasks/Canvas expose `agent_writable` and a sensitivity category, never the field value. | Pure classifier unit-test source, architecture gate, and real-browser password/card/OTP refusal test source. | SOURCE CONNECTED | PARTIAL SOURCE | Pure policy parses clean against pinned headers; PageAgent and browser test still need generated-header compilation and runtime AX/autofill proof on a capable host. Unannotated payment controls remain non-observable by value, but native Autofill field-type integration would strengthen action refusal beyond standards-conforming markup. |
| Voice | Voice session state, reference resolution, fake providers, and macOS speech/TTS adapters. | `VoiceSession` plus platform providers. | Session cancel/stop paths; platform object teardown. | Profile/window turn context. | None in source currently. | `//seoul/browser/voice`, `//seoul/browser/voice/platform`. | macOS Speech and AVFoundation adapters. | Canvas voice controls. | Unit-test source for pure voice. | SOURCE COMPLETE | PARTIAL SOURCE | Real microphone capture, permissions, and latency unverified. |
| Scenes, themes, site layers | Appearance and site customization tied to organization and routing. | Pure scene/theme/site-layer services and runtime resolvers. | Runtime and pure service shutdown. | Profile, workspace, origin. | Bounded Scene/Theme/Site Layer state in the product pref. | `//seoul/browser/scenes`, `//seoul/browser/themes`, `//seoul/browser/site_layers`. | None for pure modules. | Scene, validated Theme-preview metadata, and Site Layer metadata are visible in read-only Studio. | Unit-test source plus Studio architecture gate. | SOURCE COMPLETE | PARTIAL SOURCE | Creation/editing/activation, mutation scheduling, and browser application remain unreachable; all runtime evidence remains. |
| Threads and workflows | Save task context and successful work as editable workflows. | `ThreadService` and `WorkflowService`. | Runtime shutdown after task shutdown. | Profile. | Product pref. | `//seoul/browser/product:product_core`, `//seoul/browser/workflows:workflows_core`. | None for pure modules. | Canvas/Studio future UI. | Unit-test source. | SOURCE COMPLETE | PARTIAL SOURCE | No compiled or UI runtime evidence. |
| Packaging and release | Build, sign, notarize, package, install, performance/crash test. | Not authored as a release pipeline yet. | MISSING | Product distribution. | Release artifacts outside repo. | MISSING | MISSING | Installer/app bundle. | MISSING | MISSING | MISSING | Requires capable host, credentials, and clean-machine verification. |

Capable-host continuation command sequence:

```sh
native/scripts/verify-checkout.sh
native/scripts/build-host-check.sh
native/scripts/materialize.sh apply
native/scripts/gen.sh
native/scripts/build.sh
native/scripts/smoke.mjs
native/scripts/materialize.sh reverse
```

The command sequence must run on a host that passes the build-host guard. On
this computer the correct checkpoint remains SOURCE CONNECTED/PARTIAL SOURCE,
not COMPILED, RUNTIME VERIFIED, or RELEASE VERIFIED.
